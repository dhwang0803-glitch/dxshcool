# VOD_Embedding 세션 보고서 — 2026-03-12

## 세션 요약

| 항목 | 내용 |
|------|------|
| 날짜 | 2026-03-11 21:28 ~ 2026-03-12 07:09 |
| 작업 | 크롤링 + 임베딩 자동 루프 실행 |
| 담당 | tasks_C.json (영화 7,564 + TV드라마 2,290 + 키즈 1,654 = 11,508건) |

---

## 파이프라인 진행 현황 (2026-03-12 07:09 기준)

### STEP 1. 트레일러 크롤링
| 항목 | 값 |
|------|-----|
| 대상 | 11,508건 |
| 처리완료 | 5,040건 (43.8%) |
| 성공 | 4,936건 (97.9%) |
| 실패 | 104건 |
| 디스크 | 113.9 MB (31개 파일 대기) |
| 세션 시작 대비 증가 | +4,220건 (+36.7%p) |

### STEP 2. CLIP 임베딩
| 항목 | 값 |
|------|-----|
| 처리완료 | 8,724건 |
| 성공 | 4,513건 |
| 실패 | 4,211건 (48%) |
| 저장 배치 | 289개 (pkl) |
| 자동 루프 실행 횟수 | ~369회 (run #369) |

### STEP 3. Parquet
| 항목 | 값 |
|------|-----|
| 상태 | 미생성 (크롤링 완료 후 자동 생성 예정) |

---

## 임베딩 실패율 이슈 (48%)

- **원인**: YouTube ID `vXtRnQofIWs` 등 다수 VOD가 동일한 트레일러 파일을 공유
  - 파일이 "유효하지 않은 영상"으로 판정되어 일괄 실패
  - 실제 콘텐츠 부재(지역 차단, 삭제된 영상)로 추정
- **영향**: embed_status.json에 실패 기록 → 향후 재처리 가능
- **조치**: 현재 자동 루프는 계속 진행, 크롤링 완료 후 실패 건 재검토 필요

---

## 이번 세션 주요 작업

1. `git pull` — origin/VOD_Embedding 2커밋 반영 (ba55e88)
2. 크롤링 재개 (`crawl_trailers.py`) — 백그라운드 실행 중
3. 임베딩 수동 3회 실행 후 자동 루프(`b6tqfv3w7`) 전환
   - 루프: 60초 간격으로 대기 파일 감지 → 임베딩 → 보고서 저장 반복
   - 크롤링 완료 시 자동으로 Parquet 생성 후 종료

---

## 내일 재개 시 명령

```bash
cd "$(git rev-parse --show-toplevel)/VOD_Embedding"

# 현황 확인
conda run -n myenv python scripts/progress_report.py

# 크롤링 재개 (자동 루프가 종료된 경우)
conda run -n myenv python scripts/crawl_trailers.py --task-file data/tasks_C.json --trailers-dir data/trailers

# 임베딩 자동 루프 재시작
# (아래 while 루프를 백그라운드로 실행)
```

## 잔여 예상 시간
- 크롤링: ~12시간 (현재 속도 8.7건/분 기준)
- 임베딩: 크롤링과 병렬 자동 처리 중
